---
title: TVSDK 1.4 for Desktop HLS リリースノート
description: TVSDK for Desktop HLS リリースノートでは、 TVSDK DHLS の新機能や変更点、解決された問題、既知の問題について説明します。
contentOwner: asgupta
products: SG_PRIMETIME
topic-tags: release-notes
exl-id: 5e227c99-acf6-4b16-a35a-68e2928fdbfd
source-git-commit: 1bc2f6c230c262babf2958c32fee31afcad04c2f
workflow-type: tm+mt
source-wordcount: '5194'
ht-degree: 0%

---

# TVSDK 1.4 for Desktop HLS リリースノート {#tvsdk-for-desktop-hls-release-notes}

TVSDK for Desktop HLS リリースノートでは、 TVSDK DHLS の新機能や変更点、解決された問題、既知の問題について説明します。

## 新機能 {#new-features}

**1.4.31**

* **CRS 広告のマルチ CDN サポート**

   * デフォルトでは、すべてのトランスコードされたアセットは、Akamai 上のAdobe所有の CDN でホストされます。 最新リリースのAdobeクリエイティブ再パッケージ化サービス (CRS) では、トランスコードされたクリエイティブを、お客様が指定した複数の CDN にアップロードできます。
   * 新しい API が TVSDK に追加され、デフォルト URL が使用されていない場合に最終的な CRS クリエイティブ URL を指定できるようになりました。 これらの新しい API の使用方法については、ドキュメントを参照してください。

### 以前のリリースの新機能 {#new-features-previous}

**1.4.30**

* **請求指標**

実際の使用状況に関係なく、固定料金ではなく、使用した分のみを支払いたい場合に対応するために、Adobeは使用状況指標を収集し、これらの指標を使用して顧客に対する請求額を決定します。

**1.4.24**

* **永続的なネットワーク接続**

重要：バージョン 22 以降のAdobeFlash Playerがインストールされている必要があります。
永続的なネットワーク接続は、ネットワーク要求ごとに新しい接続を開く代わりに、複数の要求で再利用できるネットワーク接続の内部リストを作成して保存します。 永続的なネットワーク接続は、ネットワークコードの効率を高め、待ち時間を短縮します。

このリリースでは、この機能はMacのApple Safari および Mozilla Firefox ではサポートされていません。

**1.4.19**

* VPAID 広告のストリームの整合性のサポート。
* ハングの問題を修正し、Firefox 42 以降のFlashプレーヤー FP 20.0.0.267 で「ミュート」タブオプションが有効になりました。

**1.4.18**

* Primetime Desktop HLS TVSDK で、VPAID 2.0 リニアSWFクリエイティブがサポートされ、リッチなインタラクティブなインストリーム広告エクスペリエンスが可能になりました。

**1.4.10**

* **広告フォールバック、広告選択ロジックのデイジーチェイン (Zendesk #3103)** フォールバックルールが有効になっている VAST 広告（クリエイティブ）の場合、 TVSDK は無効な MIME タイプを持つ広告を空の広告として扱い、その代わりにフォールバック広告を使用しようとします。 フォールバック動作のいくつかの側面を設定できます。

詳しくは、 [VAST および VMAP 広告の広告フォールバック](../programming/tvsdk-1.4-for-android/ad-insertion/ad-fallback/android-1.4-ad-fallback.md).

**1.4.8**

* **ビデオハートビートライブラリ (VHL) がバージョン 1.5 に更新されました。**

   * ビデオ開始やビデオ/広告/チャプター開始をコンテキストデータとして使用し、メタデータを送信する機能
   * ネットワークトラフィックの削減 — ハートビートは、平均的に少なく、サイズも小さくなります。

**1.4.7**

* **オンプレミスの個別化のサポート**

別のエンドポイントに移動するためのクライアントの個別化リクエストをカスタマイズするAdobeの個別化サーバーのオンプレミスインストールのサポート。

**1.4.6**

* **AES 暗号化の例 (Flash Playerバージョン 17.0.0.134 が必要 )**

サンプルベースの AES 暗号化がサポートされるようになりました。

**1.4.2**

* **ビデオハートビートライブラリ (VHL) をバージョン 1.4.0.1 に更新しました。**

   * Adobe Analytics Video Essentials に、他の SDK やプレーヤーから様々な Analytics ユースケースをバンドルする機能が追加されました。
   * trackAdBreakStart メソッドと trackAdBreakComplete メソッドを削除することで、広告トラッキングを最適化しました。 広告ブレークは、 trackAdStart および trackAdComplete メソッド呼び出しから推論されます。
   * 広告を追跡する際に、再生ヘッドプロパティは不要になりました。

**1.4.0**

* **代替コンテンツ置換によるブラックアウトシグナリング** TVSDK は、1.4 の TVSDK の更新の一環として、リニアコンテンツに対する地域ブラックアウトへの切り替えとリピートをサポートするようになりました。 TVSDK は、2 つのマニフェストファイルを並行して、メインと代替で処理できるようになり、元のプログラミングの代わりに代替プログラミングが表示されている場合でも、ブラックアウト信号を監視できます。

* **C3 広告の削除/置換** 現在は、C3 ウィンドウから出てくる Video-on-demand(VOD) アセットに新しい広告を動的に挿入するための準備作業は追加で必要ありません。 TVSDK は、カスタムコンテンツ範囲を削除し、新しい広告を動的に挿入する API を提供するようになりました。 この強力な新機能は、放送中にライブ/リニアコンテンツの放送が発生し、アセットを「クリーンアップ」する適切な時間をかけずに、オンデマンドコンテンツとして即座にプルダウンして使用する場合にも便利です。

## 解決された問題 {#resolved-issues}

>[!NOTE]
>
>CRS を使用するすべての TVSDK ユーザーは、iOS、Android、Desktop HLS で、少なくとも TVSDK 1.4.32 にアップグレードすることを強くお勧めします。 このアップグレードは、既存のアプリケーション実装の代わりとなるドロップインです。 アップグレード後、プロキシツール（Charles など）で CRS クリエイティブ URL リクエストをチェックして、パス内のバージョンがバージョン 3.1 を反映していることを確認します。例：
>
>`https://adunit.cdn.auditude.com/assets/3p/v3.1/218747/94b/c1b/94bc1b964cc67e115a5a6781c7329b90_ee92607938ffff46b083121f044c2746.m3u8`

**バージョン 1.4.41**

* Zendesk #33777 - DHLS 配布ビルドの Localhost トークンSWFが期限切れです。

  DHLS での PMP デモ用の localhost トークンを更新します。

### 以前のリリースの問題を解決しました。 {#resolved-issues-previous}

**バージョン 1.4.38** (891)

* Zendesk #30731 - TVSDK は、AdBreak で複数の VPAID 広告を再生しません。

  AdBreak での複数の VPAID 広告の再生を修正しました。

* Zendesk #29968 - Double Billboard.

  ABR 切り替えが発生した時点で、ある期間の最後のセグメントをビデオプレーヤーが繰り返すことができます。 このため、時々、最後のプリロールのセグメントが繰り返されます。 この問題は修正されました。

**バージョン 1.4.35** (879)

* Zendesk #26058 — ネイティブ VPAID イベントをサポート

**バージョン 1.4.33** (873)

* Zendesk #21701 — 正規化された URL ではなく、1401 CRS リクエストの元のクリエイティブ URL を送信します。

  CRS バックエンドで必要とされる、トランスコード用に既に再パッケージ化された URL がリクエストされる問題を修正しました。
* Zendesk #26197 — アナモルフィック圧縮は、希望の表示解像度で再生されません。

  **注意**:この問題が発生するには、Flashプレーヤー 24.0.0.194 以降が必要です。

  縦横比テーブルに見つからないエントリを使用して出力幅が計算される問題を修正しました。

* Zendesk #26840 - HDCP 検出が 2 回試行した後、IE11 + Windows7 で失敗する。

  **注意**:この問題が発生するには、Flashプレーヤー 24.0.0.218 以降が必要です。

  この問題は、AdobeCP のメッセージキュー処理を変更して、最初のメッセージをブロックするだけでなく、キュー全体を繰り返し処理することで解決されました。

* Zendesk #27460 — 新しい Akamai アカウントがPOSTCDN リクエストを処理できません。

  新しい CDN アカウントは、POSTCDN リクエストを処理できません。 この問題は、コードを更新して cdn.auditude.com 広告リクエストをPOSTではなくGETにすることで解決されました。
* Zendesk #27619 - Windows 10 でFlashがクラッシュする

  **注意**:この問題が発生するには、Flashプレーヤー 24.0.0.218 以降が必要です。

  この問題は、長い URL の結果として発生する障害を防ぐことで解決されました。

* Zendesk #28218 — 再開ポイントからのプレースバック中にトラッキングイベントが発生しない

  この問題は、Zendesk #26592と同じ問題です。 メディアプレーヤーが VOD ストリームの PREPARED 状態にある場合に、シーク操作が許可されていた問題を修正しました。

**バージョン 1.4.32** (867)

* Zendesk #26592再生が再開ポイントから開始した場合、トラッキングイベントは発生しません

再開ポイントが 0 でない場合、コードはプリロール広告ブレーク項目を更新していませんでした。 この問題は、範囲の開始時間が一致しない場合にコードを更新するロジックを追加することで解決されました。

* Zendesk #27022 Ads は、アセットが 2 回再生されたときに再生されません

配列メソッドの例外を修正しました。

**バージョン 1.4.30** (855)

このリリースでは、 TVSDK に関する以下の問題が解決されました。

* Zendesk #22898 — 字幕が見つからない場合、再生が失敗しないようにする必要があります。

**注意**:この問題が発生するには、Flashプレーヤー 23.0.0.185 以降が必要です。

この問題は、マニフェストに WebVTT M3U8 がない場合でも TVSDK が再生を続行でき、警告を登録することで解決されました。

* Zendesk #23454 — サードパーティ広告 (VPAID) が正しく処理されない

この問題は、時間範囲ではなくコンテンツ ID に基づいて VPAID 広告を正しく処理することで解決されました。

* Zendesk #24528 - TVSDK の使用状況の指標を請求に使用します。

重要：この問題が発生するには、Flashプレーヤー 23.0.0.185 以降が必要です。

* Zendesk # 25432プレイヤーのサイズ変更時のクローズドキャプションの問題。

重要：この問題が発生するには、Flashプレーヤー 23.0.0.185 以降が必要です。

プレーヤーのサイズ変更中の座標を適切に処理するように、キャプション表示テクスチャマップコードを修正しました。

ビデオハートビートライブラリ (VHL) が、次の問題を解決するために、バージョン 1.5.9 に更新されました。

* Zendesk #23730 — ビットレート指標が空です

この問題は、VideoAnalyticsTracker でビットレートの変更を追跡することで解決されました。

* PTVideoAnalyticsTrackingMetadata に、ライブ/リニアストリームのアセット時間を更新する新しい API、assetDuration が追加されました。

**バージョン 1.4.28** (848)

* Zendesk #25027 -Auditudeは 1.4.27 デスクトップリリースでは機能しません

この問題は、AUDITUDE_METADATA_KEY を確認するコードを追加し、AUDITUDE_METADATA_KEY と ADVERTISING_METADATA_KEY を交換可能にすることで解決されました。

* Zendesk #24428 - TVSDK を使用して DRM HLS を再生する際のバッファリングの問題が頻繁に発生する

この問題は、広告の設定がない場合、広告挿入を同期するように設計されたタイムライン上のプリロール広告保留とライブ予約保留を削除することで、 TVSDK は処理を高速化できることを考慮して解決されました。

* Zendesk #24344 - WebVTT ファイルを無効にして、起動時間を短縮します。

**注意**:この問題が発生するには、Flashプレーヤー 23 以降が必要です。

この問題は、キャプションが表示される必要がある場合にのみ、WebVTT ファイルを読み込むことで解決されました。

* Zendesk #24994 - CM から戻ると、クローズドキャプションがプレーヤーからドロップされる

**注意**:この問題が発生するには、Flashプレーヤー 23 以降が必要です。

誤った EOC コードが原因で、キャプション表示が消えていました。 この問題は、現在のアクティブウィンドウで正しく表示されるように 608 キャプションコード RU2、RU3、RU4 を強制することで解決されました。

**バージョン 1.4.27** (844)

* Zendesk #21554 - application-type = video/mp4 に対して TVSDK エラービーコンが実行されません

この問題は、 TVSDK が無効なアセット形式で正しいエラー追跡 URL を ping で有効にすることで解決されました。

* Zendesk #23402 — 不完全な広告再生

**注意**:この問題が発生するには、Flashプレーヤー 23 以降が必要です。

特定のリクエストで 404 エラーが発生した後、クラッシュが発生する場合があります。 この問題は、応答の処理中に接続がシャットダウンされないようにすることで解決されました。 この問題を解決することで、VPAID 広告ファイルが正しくカウントされないので、ダウンロード中に VPAID 広告がリリースされることはありません。

* Zendesk #23621 - 400 と 404 で再試行が失敗する

**注意**:この問題が発生するには、Flashプレーヤー 23 以降が必要です。

異なるプロファイル間を切り替えると DRM メタデータが破損する問題を修正しました。

* Zendesk #23705 - AdStitched の時間中にビデオ広告がフリーズする

**注意**:この問題が発生するには、Flashプレーヤー 23 以降が必要です。

この問題は、Zendesk #23621と同じ問題です。

* Zendesk #23905 — 一部の広告ブレークが広告ブレークをスキップする

**注意**:この問題が発生するには、Flashプレーヤー 23 以降が必要です。

Windows のネイティブネットワークコードは修正され、現在他の接続で使用されているハンドルが接続で閉じられないようになりました。

* チケット#24029 - HLS FER ストリームは、ミッドロール.json ファイル内のすべてのミッドロール広告を再生しません。

この問題は、クライアントが OpportunityGenerator を上書きする必要がないように、クライアントが Opportunity インスタンスで個別にカスタムパラメーターを設定できるようにすることで解決されました。

**バージョン 1.4.26** (839)

* Zendesk #18854 - CRS ルールに基づいてクリエイティブの選択ロジックを更新
   * CRS ルールに基づいてクリエイティブの選択ロジックを更新するサポートが提供されました。

* Zendesk #22725 - Desktop のサンプルアプリケーションでの playbackManager.beginPlayback() の実装

   * メソッドが setupVideo() から呼び出されるので、startPlaybackFromFlashVars の最後でこの冗長な呼び出しを削除して修正しました。

* Zendesk #22807 - SeekManager null 参照例外

   * _dispatcher に関連する SeekManager 内に必要な NULL ポインター保護を提供することで修正しました。

* Zendesk #22822 - TVSDK を使用して明確な HLS を再生する場合に頻繁にバッファリングする

   * 広告がない場合に adSignalingModeOpportunityGenerator で生成された初期オポチュニティを削除して修正しました。

* Zendesk #23378 - Stream integrity blocks rules.xml

   * ストリーム整合性ワークフローを介して rules.xml ファイルを読み込むことで修正しました。

**バージョン 1.4.24** (817)

* Zendesk #19851 — プレーヤーが異なるビットレートに適応する場合、新しいビットレートに時間を取り戻して数フレーム戻り、不適切なエクスペリエンスを作成します

**注意**:この問題が発生するには、Flashプレーヤー 22.0.0.175 以降が必要です。

セグメントのごく一部をダウンロードした後に DRM アダプタがリセットされ、正しく復元されない問題が解決しました。

* Zendesk #20784 - Analytics:ライブビデオ切り替えのコンテンツ完了のトリガー

この問題は、LINEAR/LIVE ビデオトラッキングセッション中にコンテンツの完了を手動でトリガーする API(trackVideoComplete) を追加することで解決されました。

次のライブラリが更新されました。

* Zendesk #21643 - VPAID 広告が完全に再生されない

   * AdobeMobile ライブラリから4.10.0へ
   * VHL ライブラリから 1.5.6 へ
   * VHL-Nielsen ライブラリから 1.6.7 へ

この問題は、VPAID 広告の再生中に高さ 0 のビューポートを使用してステージを埋めることで解決されました。

* Zendesk #22110 - Analytics:h を追加:sc:ハートビートトラッキングコールへの ssl フィールド

SSL 関連の問題を修正し、TVSDK で使用される VHL ライブラリを最新バージョンに更新しました。

* Zendesk #22608 — ビデオが断続的に黒い画面を表示します (Flash Player22.0.0.175 以降が必要 )

アダプティブビットレートの間、最大ビットレート制限を持つ場合、クライアントが位置の更新を見て、クライアントがコンテンツを再生しているかのように動作する場合でも、ビデオの再読み込みに断続的に黒い画面が表示されます。

**バージョン 1.4.23** (809)

* Zendesk #2887 - TVSDK に適用された広告ルールロジックの場合のポストロール広告スキップの問題

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.240 以降が必要です。

広告ルールロジックが TVSDK に適用された際に、ポストロール広告がスキップされていた問題を修正しました。

* Zendesk #19863 - VPAID メディアファイルを破棄した広告

VPAID 広告が最初の広告である複数のメディアファイルが Vast インライン広告に含まれている場合、インライン広告はライブストリームでは再生されません。 この問題は、別のメディアファイルを代わりに取得することで解決されました。

* Zendesk #21021 - Late Binding オーディオがオーディオセグメントの繰り返しを引き起こす

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.240 以降が必要です。

オーディオの繰り返しの問題が修正されました。

* Zendesk #21125 — ライブ/リニア広告ブレークから早い段階で戻る

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.240 以降が必要です。

このリリースでは、広告の時間が再生されて完了するまでの早い時間からの復帰がサポートされています。 早期返品は、カスタムマニフェストタグを通じて示されます。

* Zendesk # 21369遅延バインディングオーディオは時間の不整合を引き起こします

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.240 以降が必要です。

修正されたオーディオの繰り返しの問題も修正されました。

* Zendesk #21760, 20921 - Audio Video Desync on Seek.

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.240 以降が必要です。

オーディオの繰り返しの問題を修正しました。

* Zendesk #22024 - TVSDK の実行中にエラーが発生しました

参照プレーヤーがストリームを再生せず、起動時に例外をスローしていた問題を修正しました。

**バージョン 1.4.22** (791)

* Zendesk #17580 - Primetime ランタイムエラー（コード 3357）

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.197 以降が必要です。

storeVoucher() が呼び出されたときに deviceID を適切に初期化することで発生したランダムな 3357 エラーが修正されました。

* Zendesk #21334 — サードパーティ広告リクエストの TVSDK 広告リクエストのタイムアウト値

このリリースでは、グローバル広告リクエストのタイムアウトが追加されました。

**バージョン 1.4.21** (782)

* Zendesk #19580 TVSDK は、コンテンツリゾルバーが完了するのを待ってから送信します。 `PTTimedMetadataChangedNotification` 通知

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.182 以降が必要です。

この問題は、Desktop Reference Player で解決されました。広告タグを設定する機能を提供し、カスタムキューへの登録方法と、VOD ファイルでのこれらのキューの処理方法を示すカスタムオポチュニティジェネレーターを追加しました。

* Zendesk #20806 DVR ウィンドウの将来のミッドロール広告が、カムのスワップ後にトリガーしない

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.182 以降が必要です。

この問題は、PIP スワップでのプリロール広告の挿入を無効にするためにアプリを更新して_resource.metadata.setValue(DefaultMetadataKeys.ENABLE_LIVE_PREROLL, &quot;false&quot;) を設定することで解決されました。その結果、プリロールオポチュニティは生成されませんでした。

並べ替え機能が導入され、シーケンスが正しくなく、メインコンテンツの時間がマイナスになる問題を修正しました。

* Zendesk #20522:VPAID 2.0 広告をスキップできません

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.182 以降が必要です。

* この問題は、広告の許しの間に VPAID 広告をスキップすることで解決されました。
* adbreak ポリシーが skip に設定されている場合、広告および広告ブレークイベントは引き続きディスパッチされます。 プレーヤーの状態が一貫していません。

この問題は、広告の時間がスキップされた場合に、イベントがディスパッチされず、正しく動作するように解決されました。

* Zendesk #20955オポチュニティジェネレーターを通じて、customParameters プロパティにキーと値のペアを設定します。

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.182 以降が必要です。

Auditudeリクエストは、広告リクエスト用の広告ユニットを作成する際に、カスタムパラメーターの AuditudeSettings を解析します。

この動作は、リクエストの Opportunity オブジェクトのカスタムパラメーターを含むように変更されました。 また、異なるカスタムパラメーターを持つ複数のオポチュニティを、1 つのAuditudeリクエストにパックすることはできません。

* Zendesk #21227 - m3u8 の一貫した再生に失敗

**注意**:この問題が発生するには、Flashプレーヤー 21.0.0.211 以降が必要です。

この問題は、TVSDK がサポートしていない AC3 コーデックを含むマニフェスト（HLS サブプロファイル）を TVSDK が無視することで解決されました。

**バージョン 1.4.20** (762)

* Zendesk #19181 - Trick play fast forward to live point locks up stream.

**注意**:この問題が発生するには、Flashプレーヤー 20.0.0.306 以降が必要です。

* Zendesk #19286 - FER ストリームでの前後のシーク中にFlash Playerのクラッシュが発生しました。

**注意**:この問題が発生するには、Flashプレーヤー 20.0.0.306 以降が必要です。

Google Chrome でのシーク時に、クエリのシャットダウンによって解決された場合、クエリが応答を取得するのに時間がかかりすぎる場合、またはソケットがシャットダウン中の場合に発生する、不定期のハング。

* Zendesk #19305 - A/V 不連続でストリームを再生中に途切れた再生が発生しました。

**注意**:この問題が発生するには、Flashプレーヤー 20.0.0.306 以降が必要です。

この問題は、警告をレポートすることで解決されました。

* Zendesk # 19359 -Flash Playerは、#EXT-X-FAXS-CMの位置が原因でクラッシュしました：属性を設定レベルのマニフェスト内に追加します。

この問題は、#EXT-X-FAXS-CMタグがプレイリスト内の個々のビットレートまたはセグメントの前の最上位のプレイリストに表示された場合に解決されました。

* Zendesk #19489 - Fast Forward to Live Point 停止プラグイン（ライブストリーム）

この問題は Zendesk #19181と同じです。

* Zendesk #19699 - TVSDK が WebVTT の字幕トラック間の切り替えに失敗する

この問題は、トラックの変更時にプレーヤーのダンプとマニフェストの再読み込みを行い、2 バイトの WebVTT キャプショントラック名に影響を与えた UTF8 文字列変換の問題を修正することで解決されました。

**バージョン 1.4.19** (1.4.19.738)

* Zendesk #18234 - CC で Unicode 文字列を持つストリームの再生中にFlash Playerがクラッシュします

この問題は、Flash PlayerFP 20.0.0.267 以降が必要で、Unicode 文字列を正しく処理することで解決されました。

* Zendesk #18304 - VPAID 広告のストリーム整合性サポート

この機能にはFlash PlayerFP 20.0.0.267 以降が必要で、リリース 1.4.19 で導入されました。

* Zendesk #18766 - Reference Player が CC トラック名に非ラテン Unicode 文字を表示できない

この機能にはFlash PlayerFP 20.0.0.267 以降が必要で、Unicode 文字列を正しく処理することで修正されました。

* Zendesk #18804 - Firefox 42 でプレーヤーがクラッシュする

この問題にはFlash PlayerFP 20.0.0.235 以降が必要で、Zendesk #18723と同じ問題です。

* Zendesk #18864 -Flash Playerフルプラグインクラッシュ

この問題は、Flash PlayerFP 20.0.0.235 以降が必要で、Zendesk #18723と同じです。

* Zendesk #18998 — オーディオとビデオのタイムスタンプが一致しない場合、不連続性に関するセグメントの無限のダウンロードが発生します。

この問題は、タイムスタンプ間のギャップを無視し、ダウンロードされたコンテンツを再生するだけで解決されました。

* Zendesk #19093 — ミッドロール広告は、ライブおよびフルイベント再生 (FER) コンテンツで 1 回のみ視聴できますが、広告の早送りやシークの通過時には、再び視聴できませんでした

Primetime のデフォルトの adPolicy セレクターでは、ミッドロール広告が視聴されている場合、シークを完了すると、adBreak はシークされた位置に移動されません。 広告を再び再生するには、シーク後に、アプリケーションが selectAdBreaksToPlay() 関数をオーバーライドする必要があります。

* Zendesk #19101 — 未解決の Midroll 広告に巻き戻すと、広告の配置が削除されます。

この問題は、プレーヤーが playbackMetrics 時間、minimumOpportunityTime および Timeline を更新できるようにすることで解決されました。

* Zendesk #19102 - FER とトリックモードの問題

この問題は、Flash PlayerFP 20.0.0.267 以降が必要で、advertisingMetadata.adSignalingMode を正しく設定することで解決されました。

* Zendesk #19175 — プリロール広告では、ストリームが初めて再生されたときに表示されないことがあります。

この問題は、広告リクエストのタイムアウト時に新しい API adRequestTimeout を AuditudeSettings に追加することで解決されました。 ユーザーは、デフォルトの 10 秒の広告リクエストタイムアウトを上書きできるようになりました。

**バージョン 1.4.18** (1.4.18.722)

* Zendesk #3324 - Primetime 広告レポートは、VMAP に広告メディアがない場合、広告の時間を追跡しません。

広告ブレークが空の場合、広告ブレーク開始および完了追跡イベントが ping されていませんでした。 この問題は、VMAP AdBreak などの空の広告ブレークに対して、有効な AdSource ノードを使用して広告ブレーク開始 ping を送信することで解決されました。

**バージョン 1.4.17** (1.4.17.702)

* Zendesk #17168 — 表示を切り替えた後、10 秒ほどキャプションが表示されない

この問題は、vtt キャプションファイルの EXT-X-MEDIA-TIME タグをサポートすることで解決されました。

* Zendesk #17983 — マニフェストのキーのダウンロードに失敗すると、マニフェスト全体の再生に失敗します

**注意**:Flash PlayerFP 19.0.0.245 以降が必要です。

ライブコンテンツを再生する場合、マニフェストに無効なキーが含まれている（例えば、ブラックアウト期間）ことがありますが、他の時間範囲は有効なキーを持ち、引き続き再生されます。 以前は、マニフェストにリストされたキーをダウンロードできなかった場合、マニフェスト全体が失敗していました。 現在は、リストされたすべてのキーをダウンロードできない場合にのみマニフェストが失敗します。 一部のキーが有効で、これらのキーの一部がダウンロードできなかった場合は、コンテンツが再生されます。 持っていないキーを必要とするセグメントを再生しようとすると、引き続き失敗します。

* Zendesk #18554 - Stream Integrity によって Cookie を切り抜く場合があります

**注意**:Flash PlayerFP 19.0.0.245 以降が必要です。

cookie 操作コードで cookie の値を切り捨てる可能性があるバグを修正しました。

**バージョン 1.4.16** (1.4.16.684)

* Zendesk #3732 - Chrome でのプロキシのサポートを追加して Stream Integrity を実現しました (Flash PlayerFP 19.0.0.207 以降が必要 )

これは拡張機能です。

* Zendesk #4244 - PTS ロールオーバー時のストリーミングの問題

この問題は、ロールオーバーを検出し、一般的ではなく、ペイロードタイプごとの不連続を管理することで解決されました。

* Zendesk #4487 — コンテンツの線形チャネルの追跡

この問題は、リニアストリーム再生セッション中にビデオハートビートトラッカーを再初期化することで解決されました。

* Zendesk #17427 - Chrome(Win7) でプロキシを介してAdobeストリームの整合性が機能しない ()

**注意**:解決には、Flash PlayerFP 19.0.0.207 以降が必要です。

この問題は Zendesk #3732と同じです。

* Zendesk #17907 - pHLS ライブストリームのラグ (Flash Player19)

**注意**:解決には、Flash PlayerFP 19.0.0.207 以降が必要です。

この問題は、ライブマニフェストを再読み込みする際に TS ファイルのドメインが変更され、ファイルが 2 回ダウンロードされたライブストリームを処理することで解決されました。

* Zendesk #17931 — 最初にスレートが付いた HLS コンテンツが再生に失敗する

**注意**:解決には、Flash PlayerFP 19.0.0.207 以降が必要です。

最初の TS ファイルの最初の 2 秒でオーディオのないストリームを処理することで、問題が解決しました。

* Zendesk #17934 -Flash19.0.0.185 でのライブストリーミング失敗

**注意**:解決には、Flash PlayerFP 19.0.0.207 以降が必要です。

この問題は、セグメントの境界でオーディオフレームとビデオフレームの間に時間の間隔を置いたライブストリームを処理することで解決されました。

* Zendesk #17973 — 最新Flash Player19.0.0.185 がミッドロール中にクラッシュした

**注意**:解決には、Flash PlayerFP 19.0.0.207 以降が必要です。

この問題は、ミッドロール広告挿入を使用した不変オーディオの処理によって解決されました。 （パーサーの切り替えが発生し、再生中の任意の時点で、コンテンツがミッドロール広告に、または広告再生の途中に遷移します。）

* Zendesk #18049 - Firefox 42 ベータ版でFlash19 がクラッシュした

この問題は Zendesk #17973と同じです。

**バージョン 1.4.15** (1.4.15.678)

* Zendesk #4377:広告ポリシーが原因で広告の時間をスキップする際に AD_BREAK_SKIPPED イベントを発生させます。

修正は、広告がスキップされた場合に AD_BREAK_SKIPPED を追加することでした。

* Zendesk #4496 - Stream Integrity:トークン化されたストリームにリダイレクトする際にエラー102100が発生しました。

修正は、 TVSDK を介して AVNetworkConfiguration プロパティ useCookieHeaderForAllRequests を設定するサポートを追加することでした。

* Zendesk #17179 -Flash・プレーヤーが、暗号化されたコンテンツに対する複数の SAP 変更でクラッシュ。

一部の暗号化されたコンテンツの再生中にクラッシュが修正されました。

**注意**:この修正には、Flash Player19.0.0.200 以降が必要です。

* Zendesk #17499 — 監視後にミッドロールを削除せず、プリロールをファーコンテンツから削除する方法

AdPolicySelector がプリロール、ミッドロール、ポストロールコンテンツに対して異なるポリシーを返すように、 AdBreakTimelineItem (AdBreakTimelineItem.placementType) にタイプを追加しました。

* Zendesk #17665 — 帯域幅制限

修正は、バッファリングが開始する際にターゲットのバッファサイズを初期のバッファサイズに変更するロジックを削除することでした。

**バージョン1.14.14** (1.4.14.771)

* Zendesk #17363 - README ドキュメントを修正し、リファレンスプレーヤー

   * playerglobal.swc のダウンロードとインストールの手順を明確にします。
   * 特定の Flash Player バージョンでプロジェクト設定を更新する手順を追加します。
   * 最小限のプレーヤーバージョンを使用するように AdvertisingOverlay プロジェクト設定を更新しました。
   * 特定のプレーヤーバージョン 11.9 を使用するように ReferenceCore プロジェクト設定を更新しました。

* Zendesk #17471 - Player Freezes

シーク後に広告が最初から再生されない問題の部分的な修正。

* Zendesk #17496 - DVR ウィンドウでのシークバック時に Podbuster が解決されない

各広告ブレークのカスタムパラメーターを指定します。

**バージョン 1.4.13** (1.4.13.660)

* Zendesk #4037 — 使用可能なプロファイルエラーなし (Flash Player18.0.0.232 以降が必要 )

クエリパラメーターに「http」が含まれる場合の url 解析の問題を修正

* Zendesk #4260 -Flash Player18 が IE11 でクラッシュしました (Flash Player18.0.0.232 以降が必要 )

IE11 で全画面表示モードでビデオを再生するとクラッシュが発生する問題を修正しました。

* Zendesk #4262 - Windows 10 でAdobe Primetime Player がクラッシュする (Flash Player18.0.0.232 以降が必要 )

Windows で FireFox を使用して全画面表示モードでビデオを再生するとクラッシュが発生する問題を修正しました。

* Zendesk #4279 - HLS TVSDK ad insertion -302 リダイレクトの問題 (iOSとデスクトップ )

拡張子がないので URL がタイプを正しく認識できない問題を修正しました

* Zendesk #4306 - Win でのみフルスクリーンに移動するとFlash Playerがクラッシュする (Flash Player18.0.0.232 以降が必要 )

Windows で全画面表示モードでビデオを再生するとクラッシュが発生する問題を修正しました。

* Zendesk #4480 - ID3 タグイベントが見つかりません (Flash Player18.0.0.232 以降が必要 )

**1.4.12 **(1.4.12.656)

* Zendesk #2751 - CSAI と CRS | Enhance:特定のメディアファイル URL の動的要素を処理します。

Creative Repackaging Service が更新され、動的なクリエイティブ URL を含む広告を適切に処理するようになりました。

* PTPLAY - 2114 - MP4 の再生をサポート。

再生、一時停止、シークを含む、MP4 コンテンツの基本再生がサポートされるようになりました。

以下にはFlash Player18.0.0.225 以降が必要です。

* Zendesk #3992 - TrickPlay の追加速度。

TrickPlay で 16 倍の料金を受け入れるようになりました。+/- 32, +/-64 and +/-128.

* Zendesk #3113 -Flash Playerプラグインのクラッシュ

Mac Firefox でリダイレクト広告を再生しようとしたときにクラッシュが発生する問題を修正しました。

* Zendesk #4037 — 使用可能なプロファイルエラーなし
* Zendesk #4262 - Adobe Primetime Player が Windows 10 でクラッシュする

全画面での再生中に Windows Firefox でクラッシュする問題を修正しました。

**バージョン 1.4.11** (1.4.11.648)

* Zendesk #1869 — フォントサイズの変更に関する問題 (Flash Player18.0.0.200 が必要 )

WebVTT キャプションコードでキャプションサイズを使用できるようにします。

* Zendesk #3113 -Flash Playerプラグインのクラッシュ (Flash Player18.0.0.200 が必要 )
* Zendesk #3268 - Desktop:ビデオプレーヤーが+- 40/50秒後にちらつきを開始し、+- 90 秒後に黒くなり始めます (Flash Player18.0.0.200 が必要 )。

ステージビデオのバグを修正しました。

* Zendesk #3670 - Reference Player でのシーク中に VOD で INVALID_PARAMETER エラーが発生しました (Flash Player18.0.0.200 が必要です )

新しい期間が検出された場合、ThreadSeek で ValidateProfiles を無効にします。

* Zendesk #3896 - Chrome で Stream Integrity が ON に設定されているとFlash Playerがクラッシュする (Flash Player18.0.0.200 が必要 )

Pepper でのネイティブネットワークモードでのクラッシュを修正しました。

* Zendesk #3905 - CDN でホストされている場合、 TVSDK プレーヤーが読み込まれない

pageDomain が swf ドメインと異なる場合にワイルドカードトークンを検索する問題を修正しました。

**バージョン 1.4.10** (1.4.10.642)

* Zendesk #3249 - TVSDK Web Player が Firefox でFlashをクラッシュする

外部モニターで再生されるストリームがより高いビットレートストリームに切り替わると、Mac上の Firefox で時々Flash Playerがクラッシュする問題を修正しました。(Flash Player18.0.0.160 が必要 )

* Zendesk #3268 - Desktop:次の後にビデオプレーヤーがちらつき始める `+-` 40/50秒後に黒くなり始めます `+-` 90 秒

Mac Chrome で、ストリームがちらつき始め、最終的に黒くなる問題を修正しました。 (Flash Player18.0.0.161 が必要 )

* Zendesk #3304 - VAST 3.0 `[ERRORCODE]` マクロが入力されていません

   * インライン広告に不適切なクリエイティブがある場合、エラーコード 400 が表示されます。
   * `[ERRORCODE]` マクロは URL エンコードされます

* Zendesk #3601 — 機能強化リクエスト：ラッパーコンパニオン管理

   * TVSDK は、リソース（html、iframe または static ）がインラインに閉じるラッパーコンパニオンを表示します。 （インラインにそのサイズの値が含まれていない場合）
   * 表示に使用しない限り、リソースとのラッパーコンパニオンは無視されます。 （トラッキングには使用しません）
   * 追跡の目的で使用されるのは、NO リソースを持つラッパーコンパニオンのみです。 （トラッキングのみを含むラッパーコンパニオン）

**バージョン 1.4.9**

* Zendesk #2615 - HLS ビューをデスクトップディスプレイから削除する際に問題が発生

clearVideo() メソッドが MediaPlayer に追加されました。 StageVideo オブジェクトから AVStream をクリアして、表示されているビデオフレームをクリアします。 ビデオが一時停止した場合にのみ呼び出し、 replaceCurrentResource または replaceCurrentItem を呼び出してから、 play() を再び呼び出す必要があります。

* Zendesk #3169 — 参照プレーヤーをAdobe Analyticsとの統合で更新

リファレンスプレーヤーが更新され、Adobe Analyticsとの統合がおこなわれました

* Zendesk #3296 - Desktop HLS TVSDK - VAST サードパーティ広告が再生されない

HLS 形式の mime タイプでは大文字と小文字が区別されていました。これは正しくなく、大文字と小文字が区別されなくなるように変更されました。

**バージョン 1.4.8**

* Zendesk #2737 - Desktop Player — エラー106000 (Flash Player17.0.0.184 が必要 )
* Zendesk #3007 -Flash Player17 に更新した後にプリロール広告が表示されない (Flash Player17.0.0.184 が必要 )
* Zendesk #3085 - Desktop HLS Player が 60 秒後に106000エラーをスローする (Flash Player17.0.0.184 が必要 )

**バージョン 1.4.7**

* Zendesk #2760 - TrickPlay モード中に DISCONTINUITY タグが無視されました (Flash Playerバージョン 17.0.0.158 が必要 )
* Zendesk #2760 - TrickPlay モード中に DISCONTINUITY タグが無視されました (Flash Playerバージョン 17.0.0.158 が必要 )

**バージョン 1.4.6**

* Zendesk #2652 — デスクトップ HLS のAuditudeドキュメント、デスクトップ HLS ドキュメントのAuditudemedia_id を明確にしました

**バージョン 1.4.5**

* Zendesk #2256 -マスタープレイリストへのアクセス、PSDK を更新し、マスタープレイリスト上のサブスクライブ済みタグに対する timedMetadata イベントをディスパッチしました。 (Flash Playerバージョン 17.0.0.134 が必要 )
* Zendesk #2417 - Player が再生開始前に字幕をダウンロードしようとしたとき、WebVTT がセグメント番号の一致に誤ったセグメント番号変数を使用していました。 バグは、0 から始まるセグメントインデックスを持つメディアに対してのみ表示されます。 (Flash Playerバージョン 17.0.0.134 が必要 )
* Zendesk #2537 - Chrome で Pepper プラグインを使用するとFlashプレーヤーがクラッシュする (Flash Playerバージョン 17.0.0.134 が必要 )
* Zendesk #2547 — アラビア語字幕：テキストは右揃えで配置する必要があります (Flash Playerバージョン 17.0.0.134 が必要です )。

**バージョン 1.4.4**

* Zendesk #1561 - Re: `[Adobe Primetime]` 更新：Desktop PSDK での PROGRAM-DATE-TIME に対する HLS クライアントベースのフェイルオーバーサポート (Flash Playerバージョン 16.0.0.305 以降が必要 )
* Zendesk #2197 - `[Ads]` 広告エラーの追跡
* Zendesk #2286 — 機能リクエスト：広告の読み込みステータスに関する情報の提供 (VPAID)
* Zendesk #2285 — 機能リクエスト：指定されたタイムアウト時間の経過後に広告をスキップ
* バグ#3921755 - OpenSSL ライブラリをFlash Playerのバージョン 1.0.1L に更新しました (Flash Playerのバージョン 16.0.0.305 以降が必要 )

**バージョン 1.4.2**

* Zendesk #1303 — クローズドキャプションの垂直方向のオフセット (Flash Playerバージョン 16.0.0.235 以降が必要、リリース予定日：（2014 年 12 月）
* Zendesk #1870 — クローズドキャプションのオン/オフ (Flash Playerバージョン 16.0.0.235 以降が必要、リリース予定日：（2014 年 12 月）
* Zendesk #2110 - VPAID 広告の実行中に全画面表示に切り替えようとした後、再生が停止する (Flash Playerバージョン 16.0.0.235 以降が必要、リリース予定日：（2014 年 12 月）
* Zendesk #2199 - `[VPAID]` 広告ブレークを過ぎたシーク時にプレーヤーが応答しない
* Zendesk #2358 - Re: `[Analytics]` チャプターデータが正しくありません

**バージョン 1.4.1**

* ブラックアウト API が更新され、Android およびiOSの実装と一貫性が保たれるようになりました。

**バージョン 1.4.0**

* Zendesk #1024 — マニフェストを介してストリームから広告を削除する機能
* Zendesk #1423 - HLS の再生エラーがFlash Playerをロックアップしています（エラーは報告されていません）
* Zendesk #1674 - ClosedCaption 表示されません。0x03 ETX コードが見つからない場合は、708 のキャプションが表示されます。

## 既知の問題 {#known-issues}

* クローズドキャプションは、ビデオが動作する必要があるので、音声のみのコンテンツでは機能しません。
ビデオがない場合、ビューポートの寸法はなく、ビューポートの寸法がない場合、キャプションのグラフィックを表示できません。
* Google Chrome では、Chrome サンドボックスの制限により、ストリームの整合性が若干低下します。
* TVSDK 1.4 では、autoPlay を無効にすると、プレーヤーが 1 分以上アイドル状態のままになると、DRM エラーが発生する場合があります。 この問題を回避するには、autoPlay を無効にしてアセットをプリロードする場合は、 `ReferenceCore.as` 内容を変えることで `onPlaybackManagerPrepared`:

```
if (_playbackManager.autoPlay) {
_playbackManager.play();
} else {
_playbackManager.play();
_playbackManager.pause();
}
```

* **バージョン 1.4.13** PTPLAY-8501 - VMAP が 2 つの直接 MP4 非トランスコード広告を返す場合、同じフォールバック広告が 2 回再生されます。

* **バージョン 1.4.2** Flash Playerバージョン 16 リリースでは、プレーヤーが空のバッファリングイベントに入った後に、ABR の「switching down」ロジックで問題が識別されました。 この問題により、プレーヤーがバッファリング状態になった後、低帯域幅環境でビットレートが切り替わらなくなります。 問題を回避するには、アプリで `BufferControlParameters.initialBufferTime` 同じ `BufferControlParameters.playbackBufferTime` バッファリング状態の間 ( つまり、 `BufferEvent.BUFFERING_BEGIN` イベント ) をリセットし、 `BufferEvent.BUFFERING_END` イベント。 この問題の修正は、Flash Playerバージョン 16 の次期パッチリリースで利用できます。

* **バージョン 1.4.0**

   * PTPLAY-1634 — 同じ Subscribed タグが、異なるライブウィンドウで異なるタイムスタンプを持っています。 ライブウィンドウを移動する場合、各ウィンドウ内の同じタグに同じタイムスタンプが割り当てられている必要があります。 ただし、同じタグのタイムスタンプが異なる場合があります。
   * PTPLAY-28 - MediaPlayer タイムラインに空の改行が含まれない。
   * 別のドメインからコンテンツをストリーミングする権限には、クロスドメインポリシーファイル (crossdomain.xml) が必要です。 [HTTP ストリーミング用の crossdomain.xml ファイルの設定](https://helpx.adobe.com/adobe-media-server/dev/configure-dynamic-streaming-live-streaming.html).
   * バグ#3694203 - DVR ストリームで、再生中のミッドロール内から別のミッドロール広告キューにシークすると、ブラウザーがフリーズする場合があります
   * バグ#3753725 - selectPolicyForSeekIntoAd は、広告の時間が視聴されている場合に考慮しません
   * バグ#3754529 — ライブ DVR ストリームでシークバックする際に、プリロール広告がストリームから削除されない
   * バグ#3761896 — 広告の再生中にシークが許可されている場合、広告マーカーはシーク後に再調整されます。 回避策は、シーク中に ITEM_UPDATED コールバックを使用しないことです
   * バグ#3779889 - Video Analytics でトリック再生の最後に到達すると、完了呼び出しはおこなわれません
   * バグ#VA-779 — ビットレート変更イベントハートビートが、Heartbeat Support Reference Implementation を使用した Enhanced Video Analytics 用に送信されない — Trick play がサンプルアプリケーションに実装されていない。

## 参考リソース {#helpful-resources}

* 完全なヘルプドキュメントは、 [Adobe Primetimeラーニングとサポート](https://helpx.adobe.com/support/primetime.html) ページ。
