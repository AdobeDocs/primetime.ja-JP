---
title: TVSDK 1.4 for Desktop HLSリリースノート
seo-title: TVSDK 1.4 for Desktop HLSリリースノート
description: TVSDK for Desktop HLSリリースノートでは、TVSDK DHLSの新機能や変更点、解決済みで既知の問題について説明します。
seo-description: TVSDK for Desktop HLSリリースノートでは、TVSDK DHLSの新機能や変更点、解決済みで既知の問題について説明します。
uuid: 84da27b7-299b-478d-88f5-ef943f0a8321
contentOwner: asgupta
products: SG_PRIMETIME
topic-tags: release-notes
discoiquuid: e4437a26-9454-4da1-ae87-0fce664aac3d
translation-type: tm+mt
source-git-commit: a94150abc2afff4af24ee83573e73124f8b3260a

---


# TVSDK 1.4 for Desktop HLSリリースノート {#tvsdk-for-desktop-hls-release-notes}

TVSDK for Desktop HLSリリースノートでは、TVSDK DHLSの新機能や変更点、解決済みの既知の問題について説明します。

## 新機能 {#new-features}

**1.4.31**

* **CRS広告のマルチCDNのサポート**

   * デフォルトでは、すべてのトランスコードされたアセットは、Akamai上のアドビ所有のCDNでホストされます。 最新のリリースでは、Adobe Creative Repackaging Service(CRS)を使用して、トランスコードされたクリエイティブを、お客様の指定に従って複数のCDNにアップロードできます。
   * 新しいAPIがTVSDKに追加され、デフォルトURLが使用されていない場合に最終的なCRSクリエイティブURLを指定できるようになりました。 これらの新しいAPIの使用方法については、ドキュメントを参照してください。

### 以前のリリースの新機能 {#new-features-previous}

**1.4.30**

* **請求指標**

実際の使用に関係なく、使用した分のみの支払いを行いたい顧客に対応するために、アドビでは使用状況指標を収集し、これらの指標を使用して顧客に対する請求額を決定します。

**1.4.24**

* **永続的なネットワーク接続**

重要：Adobe Flash Playerバージョン22以降がインストールされている必要があります。
永続的なネットワーク接続は、複数の要求に対して再利用できる内部ネットワーク接続のリストを作成し、格納します。各ネットワーク要求に対して新しい接続を開く代わりに使用できます。 持続的なネットワーク接続は、ネットワークコードの効率を高め、遅延を減らす必要があります。

このリリースでは、Mac上のApple SafariおよびMozilla Firefoxではこの機能はサポートされていません。

**1.4.19**

* VPAID広告のストリーム整合性のサポート。
* ハングの問題を修正し、Firefox 42以降用のFlash Player FP 20.0.0.267でミュートタブのオプションを有効にしました。

**1.4.18**

* Primetime Desktop HLS TVSDKは、VPAID 2.0 Linear SWFクリエイティブをサポートし、リッチなインタラクティブなインタラクティブストリーム広告エクスペリエンスを有効にします。

**1.4.10**

* **広告のフォールバック、デイジーによる広告選択ロジックのチェーン(Zendesk #3103)** ：フォールバックルールが有効なVAST広告（クリエイティブ）の場合、TVSDKは無効なMIMEタイプの広告を空の広告として扱い、代わりにフォールバック広告を使用しようとします。 フォールバック動作の一部を設定できます。

詳しくは、VAST広告とVMAP広告の [広告のフォールバックを参照してください](../programming/tvsdk-1.4-for-android/ad-insertion/ad-fallback/android-1.4-ad-fallback.md)。

**1.4.8**

* **バージョン1.5に更新されたビデオハートビートライブラリ(VHL)**

   * ビデオ開始およびビデオ/広告/チャプター開始をコンテキストデータとしてメタデータを送信可能
   * ネットワークトラフィックの削減 — ハートビート数は平均的に少なく、サイズも小さくなります。

**1.4.7**

* **オンプレミス個別化のサポート**

別のエンドポイントに移動するためにクライアントの個別化要求をカスタマイズするための、Adobe Individualization Serverのオンプレミスインストールのサポート。

**1.4.6**

* **AES暗号化の例（Flash Playerバージョン17.0.0.134が必要）**

サンプルベースのAES暗号化がサポートされるようになりました。

**1.4.2**

* **バージョン1.4.0.1へのビデオハートビートライブラリ(VHL)の更新**

   * 他のSDKやプレーヤーから、Adobe Analytics Video Essentialsに様々な解析の使用例をバンドルする機能を追加しました。
   * 広告トラッキングは、trackAdBreakStartメソッドとtrackAdBreakCompleteメソッドを削除することで最適化されました。 広告の時間は、trackAdStartおよびtrackAdCompleteメソッドの呼び出しから推論されます。
   * 広告を追跡する際に、再生ヘッドのプロパティは不要になりました。

**1.4.0**

* **代替コンテンツの置き換えを使用したブラックアウトシグナリング** 1.4 TVSDKの更新の一環として、TVSDKは、リニアコンテンツに対する地域のブラックアウトへの移動およびリピートもサポートするようになりました。 TVSDKは、2つのマニフェストファイルをメインと代替の並行処理で処理し、元のプログラミングの代わりに代替プログラミングが表示されている場合でも、ブラックアウトシグナルを監視できるようになりました。

* **C3広告の削除** /置換今すぐ。C3ウィンドウから取得されるビデオオンデマンド(VOD)アセットに新しい広告を動的に挿入するための準備作業は不要です。 TVSDKは、カスタムコンテンツ範囲を削除し、新しい広告を動的に挿入するAPIを提供するようになりました。 この強力な新機能は、放送中にライブ/リニアコンテンツの配送を行い、適切な時間がかからずにオンデマンドコンテンツとして即座に取り出されてアセットを「クリーンアップ」する場合にも役立ちます。

## 解決された問題 {#resolved-issues}

>[!NOTE]
>
>CRSを使用するすべてのTVSDKのお客様は、iOS、Android、Desktop HLSで、少なくともTVSDK 1.4.32にアップグレードすることを強くお勧めします。 このアップグレードは、既存のアプリの実装に代わるものです。 アップグレード後、プロキシツール（Charlesなど）でCRSクリエイティブURLリクエストを確認し、パス内のバージョンがバージョン3.1を反映していることを確認します。例えば、
>
>`https://adunit.cdn.auditude.com/assets/3p/v3.1/218747/94b/c1b/94bc1b964cc67e115a5a6781c7329b90_ee92607938ffff46b083121f044c2746.m3u8`

**バージョン1.4.41**

* Zendesk #33777 - DHLS配布ビルド用のLocalhostトークンSWFの有効期限が切れました。

   DHLS上のPMPデモ用にlocalhostトークンを更新します。

### 以前のリリースで解決された問題 {#resolved-issues-previous}

**バージョン1.4.38** (891)

* Zendesk #30731 - TVSDKがAdBreakで複数のVPAID広告を再生しない。

   AdBreakでの複数のVPAID広告の再生を修正。

* Zendesk #29968 — 二重ビルボード。

   ビデオプレーヤーは、ABR切り替えが発生したときに、ある期間の最後のセグメントを繰り返すことができます。 このため、時折、プリロールの最後のセグメントが繰り返されます。 この問題は修正されました。

**バージョン1.4.35** (879)

* Zendesk #26058 — ネイティブVPAIDイベントをサポート

**バージョン1.4.33** (873)

* Zendesk #21701 — 正規化されたURLの代わりに、1401 CRSリクエスト用の元のクリエイティブURLを送信します。

   CRSバックエンドの要求に従って、既に再パッケージ化されたURLがトランスコードの要求を受ける問題が修正されました。
* Zendesk #26197 — アナモフィック圧縮が希望の表示解像度で再生されない。

   **注意**:この問題にはFlash Player 24.0.0.194以降が必要です。

   出力幅の計算に縦横比テーブルのエントリが見つからない場合に、エントリが使用される問題が修正されました。

* Zendesk #26840:2回目の試行後、IE11 + Windows7でHDCP検出が失敗する。

   **注意**:この問題にはFlash Player 24.0.0.218以降が必要です。

   この問題は、最初のメッセージを単にブロックするのではなく、AdobeCPのメインメッセージキュー処理を変更して、キュー全体を繰り返し処理することで解決されました。

* Zendesk #27460 — 新しいAkamaiアカウントでPOST CDN要求を処理できません。

   新しいCDNアカウントでPOST CDN要求を処理できない。 この問題は、コードを更新して、cdn.auditude.com広告リクエストをPOSTではなくGETにすることで解決されました。
* Zendesk #27619 - Windows 10でのFlashクラッシュ

   **注意**:この問題にはFlash Player 24.0.0.218以降が必要です。

   この問題は、長いURLの結果として発生するエラーを防ぐことで解決されました。

* Zendesk #28218 — 再開ポイントからのプレースバック中に、トラッキングイベントが発生しない

   この問題は、Zendesk #26592と同じ問題です。 メディアプレイヤーがVODストリームのPREPARED状態の場合にシーク操作が許可される問題が修正されました。

**バージョン1.4.32** (867)

* Zendesk #26592再生が再開ポイントから始まると、トラッキングイベントが発生しない

再開ポイントがゼロでない場合、コードがプリロール広告の時間項目を更新していませんでした。 この問題は、範囲の開始時間が一致しない場合にコードを更新するロジックを追加することで解決されました。

* Zendesk #27022アセットが2回目に再生されたときに広告が再生されない

配列メソッドの例外が修正されました。

**バージョン1.4.30** (855)

TVSDKのこのリリースで解決された問題は、次のとおりです。

* Zendesk #22898：サブタイトルが見つからない場合、再生が失敗することはありません。

**注意**:この問題にはFlash Player 23.0.0.185以降が必要です。

この問題は、マニフェストにWebVTT M3U8がない場合でも、TVSDKが再生を続行でき、警告を登録することで解決されました。

* Zendesk #23454 — サードパーティ広告(VPAID)が正しく処理されない

この問題は、時間範囲ではなく、コンテンツIDに基づいてVPAID広告を正しく処理することで解決されました。

* Zendesk #24528 — 請求のTVSDK使用指標。

重要：この問題にはFlash Player 23.0.0.185以降が必要です。

* Zendesk # 25432プレーヤーのサイズ変更中のクローズドキャプションの問題。

重要：この問題にはFlash Player 23.0.0.185以降が必要です。

キャプション表示テクスチャマップコードは、プレイヤーのサイズ変更時に座標を正しく処理するように修正されました。

ビデオハートビートライブラリ(VHL)がバージョン1.5.9に更新され、次の問題を解決しました。

* Zendesk #23730 — ビットレート指標が空です

この問題は、VideoAnalyticsTrackerでビットレートの変更を追跡することで解決されました。

* ライブ/リニアストリームのアセットの継続時間を更新する新しいAPI、assetDurationをPTVideoAnalyticsTrackingMetadataに追加しました。

**バージョン1.4.28** (848)

* Zendesk #25027 - Auditudeが1.4.27デスクトップリリースで機能しない

この問題は、AUDITUDE_METADATA_KEYをチェックするコードを追加し、AUDITUDE_METADATA_KEYとADVERTISING_METADATA_KEYを交換可能にすることで解決されました。

* Zendesk #24428:TVSDKを使用してDRM HLSを再生する際に頻繁にバッファリングの問題が発生する

この問題は、広告が設定されていない場合に、広告挿入を同期するように設計されたタイムライン上のプリロール広告保留とライブ予約保留を排除することで、TVSDKの処理を高速化できることを考慮して解決されました。

* Zendesk #24344 - WebVTTファイルを無効にして、起動時間を改善します。

**注意**:この問題にはFlash Player 23以降が必要です。

この問題は、キャプションを表示する必要がある場合にのみWebVTTファイルを読み込むことで解決されました。

* Zendesk #24994 — 商用休暇から戻ると、クローズドキャプションがプレイヤーから削除される

**注意**:この問題にはFlash Player 23以降が必要です。

誤ったEOCコードが原因で、キャプションの表示が消えていました。 この問題は、608キャプションコードRU2、RU3、およびRU4を、現在のアクティブなウィンドウで正しく表示するように強制することで解決されました。

**バージョン1.4.27** (844)

* Zendesk #21554 - TVSDKエラービーコンがアプリケーションタイプ= video/mp4に対して起動しない

この問題は、TVSDKが無効なアセット形式で正しいエラー追跡URLをpingできるようにすることで解決されました。

* Zendesk #23402 — 広告の再生が不完全です

**注意**:この問題にはFlash Player 23以降が必要です。

特定の要求で404エラーが発生した後、クラッシュが発生する場合があります。 この問題は、応答の処理中に接続が停止しないようにすることで解決されました。 この解決により、VPAID広告ファイルが正しくカウントされないので、ダウンロード中にファイルが解放されることはありません。

* Zendesk #23621 - 400と404で再試行が失敗する

**注意**:この問題にはFlash Player 23以降が必要です。

異なるプロファイル間で切り替えるとDRMメタデータが破損する問題を修正しました。

* Zendesk #23705 — 広告ステッチの時間中にビデオ広告がフリーズする

**注意**:この問題にはFlash Player 23以降が必要です。

この問題は、Zendesk #23621と同じ問題です。

* Zendesk #23905 — 一部の広告の時間は広告の時間をスキップします。

**注意**:この問題にはFlash Player 23以降が必要です。

他の接続で現在使用されているハンドルが接続で閉じられないように、Windowsのネットワークコードが修正されました。

* チケット#24029:HLS FERストリームは、ミッドロール.jsonファイル内のすべてのミッドロール広告を再生しません。

この問題は、クライアントがOpportunityインスタンスで個別にカスタムパラメーターを設定できるようにし、クライアントがOpportunityGeneratorを上書きする必要がなくなるようにすることで解決されました。

**バージョン1.4.26** (839)

* Zendesk #18854 - CRSルールに基づいてクリエイティブ選択ロジックを更新
   * CRSルールに基づいてクリエイティブの選択ロジックを更新するサポートを提供

* Zendesk #22725 — デスクトップ用のサンプルアプリケーションでのplaybackManager.beginPlayback()の実装

   * メソッドがsetupVideo()から呼び出されるので、startPlaybackFromFlashVarsの最後でこの重複した呼び出しを削除することで修正されました。

* Zendesk #22807 - SeekManagerの参照がnullの例外です

   * _dispatcherに関連するSeekManager内で必要なNULLポインタ保護を指定することで修正。

* Zendesk #22822 - TVSDKを使用して明確なHLSを再生する場合の頻繁なバッファリング

   * 広告がない場合に、adSignalingModeOpportunityGeneratorによって生成された初期オポチュニティを削除することで修正。

* Zendesk #23378 — ストリームの整合性ブロックrules.xml

   * ストリーム整合性ワークフローを通じてrules.xmlファイルを読み込むことで修正

**バージョン1.4.24** (817)

* Zendesk #19851 — プレーヤーが別のビットレートに適応すると、新しいビットレートで数フレーム前にジャンプし、不器用なエクスペリエンスを作り出します。

**注意**:この問題にはFlash Player 22.0.0.175以降が必要です。

セグメントの一部をダウンロードした後にDRMアダプターがリセットされ、正しく復元されない問題が解決しました。

* Zendesk #20784 — 解析：ライブビデオトランジションのコンテンツ完了のトリガー

この問題は、LINEAR/LIVEビデオトラッキングセッション中にAPI(trackVideoComplete)を追加して、コンテンツの完了を手動でトリガーすることで解決されました。

次のライブラリが更新されました。

* Zendesk #21643 - VPAID広告が完全に再生されない

   * AdobeMobileライブラリを4.10.0に
   * VHLライブラリから1.5.6
   * VHL-Nielsenライブラリから1.6.7

この問題は、VPAID広告の再生中に、高さ0のビューポートを使用してステージ全体に表示することで解決されました。

* Zendesk #22110 — 解析：h:sc:sslフィールドをハートビートトラッキングコールに追加します。

SSL関連の問題が修正され、TVSDKで使用されるVHLライブラリが最新バージョンに更新されました。

* Zendesk #22608：ビデオが断続的に黒い画面を表示する（Flash Player 22.0.0.175以降が必要）

アダプティブビットレート中、最大ビットレート制限を使用すると、クライアントが位置の更新を見て、クライアントがコンテンツを再生しているかのように動作するにもかかわらず、ビデオの再読み込みが断続的に黒い画面を表示します。

**バージョン1.4.23** (809)

* Zendesk #2887 — 広告ルールロジックがTVSDKに適用された場合のポストロール広告スキップの問題

**注意**:この問題にはFlash Player 21.0.0.240以降が必要です。

広告ルールロジックがTVSDKに適用された場合に、ポストロール広告がスキップされていた問題が修正されました。

* Zendesk #19863 - VPAIDメディアファイルを破棄した広告

大きなインライン広告に、最初の広告がVPAID広告である複数のメディアファイルが含まれている場合、ライブストリームではインライン広告は再生されません。 この問題は、別のメディアファイルを取得することで解決されました。

* Zendesk #21021 — 遅延バインディングオーディオにより、オーディオセグメントが繰り返される

**注意**:この問題にはFlash Player 21.0.0.240以降が必要です。

オーディオの繰り返しの問題が修正されました。

* Zendesk #21125 — ライブ/リニア広告の時間から早い段階で戻る

**注意**:この問題にはFlash Player 21.0.0.240以降が必要です。

このリリースでは、広告の時間が再生されて完了する前の早い時間からの再訪をサポートします。 早期の返却は、カスタムマニフェストタグを通じて示されます。

* Zendesk # 21369遅延バインディングオーディオが原因で時間が一定でない

**注意**:この問題にはFlash Player 21.0.0.240以降が必要です。

修正されたオーディオの繰り返しの問題も修正されました。

* Zendesk #21760, 20921 - Audio Video Desync on Seek。

**注意**:この問題にはFlash Player 21.0.0.240以降が必要です。

オーディオの繰り返しの問題が修正されました。

* Zendesk #22024 - TVSDKの実行時にエラーが発生しました。

リファレンスプレイヤーがストリームを再生せず、起動時に例外をスローしていた問題が修正されました。

**バージョン1.4.22** (791)

* Zendesk #17580 - Primetimeランタイムエラー（コード3357）

**注意**:この問題にはFlash Player 21.0.0.197以降が必要です。

storeVoucher()が呼び出されたときにdeviceIDを適切に初期化することで発生した3357のランダムなエラーが修正されました。

* Zendesk #21334 — サードパーティ広告リクエストのTVSDK広告リクエストタイムアウト値

このリリースでは、グローバル広告リクエストのタイムアウトが追加されました。

**バージョン1.4.21** (782)

* Zendesk #19580 TVSDKは、通知を送信する前に、コンテンツリゾルバーの完了を待ちま `PTTimedMetadataChangedNotification` す。

**注意**:この問題にはFlash Player 21.0.0.182以降が必要です。

この問題は、Desktop Reference Playerで、広告タグを設定し、カスタムキューのサブスクライブ方法と、VODファイルでのこれらのキューの処理方法を示すカスタムオポチュニティジェネレーターを追加する機能を提供することで解決されました。

* Zendesk #20806 DVRウィンドウの将来のミッドロール広告は、カムの入れ替え後にトリガーされません。

**注意**:この問題にはFlash Player 21.0.0.182以降が必要です。

この問題は、アプリを更新して_resource.metadata.setValue(DefaultMetadataKeys.ENABLE_LIVE_PREROLL, &quot;false&quot;)を設定し、PIPスワップでのプリロール広告の挿入を無効にし、その結果、プリロールオポチュニティが生成されないことで解決されました。

メインコンテンツの時間がマイナスになる原因となっていたシーケンス外の広告の配置を修正するために、並べ替え機能が導入されました。

* ゼンデスク#20522:VPAID 2.0広告をスキップできません

**注意**:この問題にはFlash Player 21.0.0.182以降が必要です。

* この問題は、広告の許容中にVPAID広告をスキップすることで解決されました。
* adbreakポリシーがskipに設定されている場合、広告および広告の時間イベントは引き続きディスパッチされます。 プレイヤーの状態が一致しません。

この問題は、広告の時間がスキップされた場合にイベントがディスパッチされない、正しく動作するように解決されました。

* Zendesk #20955オポチュニティジェネレーターを通じて、customParametersプロパティにキーと値のペアを設定します。

**注意**:この問題にはFlash Player 21.0.0.182以降が必要です。

広告リクエストの広告単位を作成する際、Auditudeリクエストは、カスタムパラメーターのAuditudeSettingsを解析します。

この動作は、Opportunityオブジェクトのカスタムパラメーターをリクエストに含めるように変更されました。 また、異なるカスタムパラメーターを持つ複数のオポチュニティを1つのAuditudeリクエストにパックすることはできません。

* Zendesk #21227 - m3u8の再生が一貫して失敗する

**注意**:この問題にはFlash Player 21.0.0.211以降が必要です。

この問題は、TVSDKがサポートしないAC3コーデック（サラウンド）を含むマニフェスト（HLSサブプロファイル）をTVSDKが無視することを許可することで解決されました。

**バージョン1.4.20** (762)

* Zendesk #19181 — リック再生が早くなり、ライブポイントがストリームをロックします。

**注意**:この問題にはFlash Player 20.0.0.306以降が必要です。

* Zendesk #19286 - FERストリーム内をシークしている間に、Flash Playerのクラッシュが発生しました。

**注意**:この問題にはFlash Player 20.0.0.306以降が必要です。

Google Chromeでのシーク中に発生したハングは、クエリをシャットダウンすることで解決されました。クエリが応答を取得するのに時間がかかりすぎる場合や、ソケットがシャットダウン中の場合です。

* Zendesk #19305 — 音声ビデオの不連続性を持つストリームを再生中に、途切れ途切れの再生が発生しました。

**注意**:この問題にはFlash Player 20.0.0.306以降が必要です。

この問題は、警告をレポートすることで解決されました。

* Zendesk # 19359 - #EXT-X-FAXS-CMの位置が原因でFlash Playerがクラッシュしました。属性を設定レベルマニフェストに含めます。

この問題は、#EXT-X-FAXS-CMタグが最上位のプレイリストに表示されてから、プレイリスト内の個々のビットレートまたはセグメントが表示されるまでに解決されました。

* Zendesk #19489 - Fast Forward to Live Point停止プラグイン（ライブストリーム）

この問題はZendesk #19181と同じです。

* Zendesk #19699:TVSDKがWebVTTの字幕トラックを切り替えられない

この問題は、トラックが変更された場合にプレイヤーでマニフェストをダンプおよび再読み込みし、2バイトのWebVTTキャプショントラック名に影響を与えたUTF8文字列変換の問題を修正することで解決されました。

**バージョン1.4.19** (1.4.19.738)

* Zendesk #18234 - CCでUnicode文字列を使用してストリームを再生すると、Flash Playerがクラッシュする

この問題は、Flash Player FP 20.0.0.267以降が必要で、Unicode文字列を正しく処理することで解決されました。

* Zendesk #18304 - VPAID広告のストリーム整合性サポート

この機能を使用するには、Flash Player FP 20.0.0.267以降が必要で、リリース1.4.19で導入されました。

* Zendesk #18766 — 参照プレイヤーで、CCトラック名に非ラテン系Unicode文字を表示できない

この機能にはFlash Player FP 20.0.0.267以降が必要で、Unicode文字列を正しく処理することで修正されました。

* Zendesk #18804 - Firefox 42でPlayerがクラッシュする

この問題にはFlash Player FP 20.0.0.235以降が必要で、Zendesk #18723と同じ問題です。

* Zendesk #18864 - Flash Playerのフルプラグインがクラッシュしました。

この問題にはFlash Player FP 20.0.0.235以降が必要で、Zendesk #18723と同じです。

* Zendesk #18998 — オーディオとビデオのタイムスタンプが一致しない場合、不連続性に関するセグメントの無限のダウンロードが発生しました。

この問題は、タイムスタンプ間のギャップを無視して、ダウンロードしたコンテンツを再生するだけで解決されました。

* Zendesk #19093：ミッドロール広告は、ライブおよびフルイベント再生(FER)コンテンツで1回のみ視聴できますが、早送りまたは広告を越えたシーク時に、再び視聴できませんでした。

PrimetimeのデフォルトのadPolicyセレクターで、ミッドロール広告が視聴された場合、シークの完了時にadBreakはシーク位置に移動されません。 再度広告を再生するには、シーク後に、アプリケーションがselectAdBreaksToPlay()関数をオーバーライドする必要があります。

* Zendesk #19101 — 未解決のミッドロール広告に巻き戻すと、広告の配置が削除されます。

この問題は、プレーヤーがplaybackMetrics時間、minimumOpportunityTimeおよびタイムラインを更新できるようにすることで解決されました。

* Zendesk #19102 - FERとトリックモードの問題

この問題は、Flash Player FP 20.0.0.267以降が必要で、advertisingMetadata.adSignalingModeを正しく設定することで解決されました。

* Zendesk #19175：ストリームを初めて再生したときに、プリロール広告に表示されない場合があります。

この問題は、広告リクエストのタイムアウトに対して新しいAPIであるadRequestTimeoutをAuditudeSettingsに追加することで解決されました。 ユーザーは、デフォルトの10秒の広告リクエストのタイムアウトを上書きできるようになりました。

**バージョン1.4.18** (1.4.18.722)

* Zendesk #3324 - VMAPに広告メディアがない場合、Primetimeの広告レポートで広告の時間が追跡されません。

広告の時間が空の場合、広告の時間の開始と完了の追跡イベントに対するpingが行われていませんでした。 この問題は、VMAP AdBreakなどの空の広告の時間に対して、有効なAdSourceノードを使用して広告の時間の開始pingを送信することで解決されました。

**バージョン1.4.17** (1.4.17.702)

* Zendesk #17168 — 表示を切り替えた後、キャプションが10秒ほど表示されない

この問題は、vttキャプションファイルのEXT-X-MEDIA-TIMEタグのサポートを提供することで解決されました。

* Zendesk #17983 — マニフェストのキーをダウンロードできないと、マニフェスト全体の再生に失敗します。

**注意**:Flash Player FP 19.0.0.245以上が必要です。

ライブコンテンツを再生する場合、マニフェストに無効なキーが含まれている可能性があります（例えば、ブラックアウト期間）が、他の時間範囲に有効なキーが含まれていて、再生が続行されます。 以前は、マニフェストに含まれるキーをダウンロードできなかった場合、マニフェスト全体が失敗していました。 現在は、一覧に表示されたすべてのキーをダウンロードできない場合にのみマニフェストが失敗します。 一部のキーが有効で、一部のキーをダウンロードできなかった場合は、コンテンツが再生されます。 存在しないキーを必要とするセグメントを再生しようとすると、失敗します。

* Zendesk #18554 — 場合によっては、ストリームの整合性によってCookieがトリミングされる

**注意**:Flash Player FP 19.0.0.245以上が必要です。

cookieの値を切り捨てる可能性があるcookie操作コードのバグが修正されました。

**バージョン1.4.16** (1.4.16.684)

* Zendesk #3732 - Chromeでのプロキシのサポートを追加してストリームの整合性を確保します（Flash Player FP 19.0.0.207以降が必要）。

これは拡張された機能です。

* Zendesk #4244 - PTSロールオーバー時のストリーミングの問題

この問題は、ロールオーバーを検出し、一般的ではなく、ペイロードタイプごとの不連続性を管理することで解決されました。

* Zendesk #4487 — コンテンツのリニアチャネルの追跡

この問題は、リニアストリーム再生セッション中にビデオハートビートトラッカーを再初期化することで解決されました。

* Zendesk #17427 - Chrome上のプロキシを通じてAdobe Stream Integrityが機能しない(Win7)

**注意**:解像度には、Flash Player FP 19.0.0.207以降が必要です。

この問題はZendesk #3732と同じです。

* Zendesk #17907 - Flash Player 19を使用したpHLSライブストリームの遅延

**注意**:解像度には、Flash Player FP 19.0.0.207以降が必要です。

この問題は、ライブマニフェストを再読み込みする際にTSファイルのドメインが変更されるライブストリームを処理し、ファイルが2回ダウンロードされることで解決されました。

* Zendesk #17931 — 開始時にスレートを含むHLSコンテンツの再生に失敗する

**注意**:解像度には、Flash Player FP 19.0.0.207以降が必要です。

この問題は、最初のTSファイルの最初の2秒間にオーディオのないストリームを処理することで解決されました。

* Zendesk #17934 - Flash 19.0.0.185を使用したライブストリーミングの失敗

**注意**:解像度には、Flash Player FP 19.0.0.207以降が必要です。

この問題は、セグメントの境界でオーディオとビデオのフレーム間のタイムインターリーブを使用してライブストリームを処理することで解決されました。

* Zendesk #17973 — ミッドロール中に最新のFlash Player 19.0.0.185がクラッシュする

**注意**:解像度には、Flash Player FP 19.0.0.207以降が必要です。

この問題は、ミッドロール広告の挿入でミュクスされていないオーディオを処理することで解決されました。 （パーサーの切り替えが発生し、再生中の任意の時点で、コンテンツがミッドロール広告に、または広告再生の途中などに移行します）。

* Zendesk #18049 - Firefox 42ベータ版でFlash 19がクラッシュする

この問題はZendesk #17973と同じです。

**バージョン1.4.15** (1.4.15.678)

* ゼンデスク#4377:広告ポリシーが原因で広告の時間をスキップする場合は、AD_BREAK_SKIPPEDイベントを発生させます。

広告がスキップされた場合にAD_BREAK_SKIPPEDを追加する問題が修正されました。

* Zendesk #4496 — ストリームの整合性：トークン化されたストリームにリダイレクトするエラー102100。

TVSDKを通じて、AVNetworkConfigurationプロパティのuseCookieHeaderForAllRequestsの設定のサポートを追加することが修正されました。

* Zendesk #17179 - Flash Playerが、暗号化されたコンテンツに対して複数のSAPの変更でクラッシュしました。

暗号化されたコンテンツの再生中に発生するクラッシュが修正されました。

**注意**:この修正には、Flash Player 19.0.0.200以降が必要です。

* Zendesk #17499 — 監視後にミッドロールを削除せず、プリロールをコンテンツの転送から削除する方法

AdPolicySelectorがプリロール、ミッドロール、ポストロールコンテンツに対して異なるポリシーを返せるように、AdBreakTimelineItem (AdBreakTimelineItem.placementType)にタイプを追加しました。

* Zendesk #17665 — 帯域幅スロットリング

修正は、バッファリングが開始される際に、ターゲットのバッファサイズを初期のバッファサイズに変更するロジックを削除することでした。

**バージョン1.14.14** (1.4.14.771)

* Zendesk #17363 — リファレンスプレイヤーのREADMEドキュメントを修正しました。

   * playerglobal.swcのダウンロードとインストールの手順を明確にします。
   * 特定のFlash Playerバージョンでプロジェクト設定を更新する手順を追加します。
   * 最小限のプレーヤーバージョンを使用するようにAdvertisingOverlayプロジェクト設定を更新します。
   * 特定のプレーヤーバージョン11.9を使用するようにReferenceCoreプロジェクト設定を更新

* Zendesk #17471 — プレイヤーがフリーズ

シーク後に広告が最初から再生されない問題の部分的な修正。

* Zendesk #17496:DVRウィンドウでシークバックしてもポッドバスターが解決されない

各広告の時間のカスタムパラメーターを指定します。

**バージョン1.4.13** (1.4.13.660)

* Zendesk #4037 — 使用可能なプロファイルエラーがありません（Flash Player 18.0.0.232以降が必要です）

クエリパラメーターに「http」が含まれる場合のURL解析の問題を修正しました。

* Zendesk #4260 - IE11でFlash Player 18がクラッシュする（Flash Player 18.0.0.232以降が必要）

IE11でビデオをフルスクリーンモードで再生するとクラッシュが発生する問題を修正しました。

* Zendesk #4262 - Windows 10でAdobe Primetime Playerがクラッシュする（Flash Player 18.0.0.232以降が必要）

WindowsのFireFoxでビデオをフルスクリーンモードで再生するとクラッシュする問題を修正しました。

* Zendesk #4279 - HLS TVSDK ad insertion -302 redirect issue on iOS and Desktop

URLに拡張子がないので、URLのタイプが正しく認識されない問題を修正しました

* Zendesk #4306 - Winのみでフルスクリーン表示するとFlash Playerがクラッシュする（Flash Player 18.0.0.232以降が必要）

Windowsでビデオをフルスクリーンモードで再生するとクラッシュする問題を修正しました。

* Zendesk #4480 - ID3タグイベントが見つかりません（Flash Player 18.0.0.232以降が必要）

**1.4.12 **(1.4.12.656)

* Zendesk #2751 - CSAIとCRS|画質調整：特定のメディアファイルURLの動的要素を処理します。

動的なクリエイティブURLを含む広告を適切に処理するように、Creative Repackaging Serviceを更新しました。

* PTPLAY - 2114 - MP4再生のサポート。

再生、一時停止、シークなど、MP4コンテンツの基本再生がサポートされるようになりました。

次の条件を満たすには、Flash Player 18.0.0.225以降が必要です。

* Zendesk #3992 - TrickPlayの追加速度。

TrickPlayで16倍を超えるレートを受け入れるようになりました。+/- 32、+/-64および+/-128。

* Zendesk #3113 - Flash Playerプラグインのクラッシュ

Mac Firefoxでリダイレクト広告を再生しようとするとクラッシュが発生する問題を修正しました。

* Zendesk #4037 — 使用可能なプロファイルエラーがありません
* Zendesk #4262 - Windows 10でAdobe Primetime Playerがクラッシュする

フルスクリーンでの再生中にWindows Firefoxでクラッシュが発生する問題を修正しました。

**バージョン1.4.11** (1.4.11.648)

* Zendesk #1869 — フォントサイズの変更に関する問題（Flash Player 18.0.0.200が必要）

WebVTTキャプションコードでのキャプションサイズの使用を許可します。

* Zendesk #3113 - Flash Playerプラグインのクラッシュ（Flash Player 18.0.0.200が必要）
* Zendesk #3268 — デスクトップ：ビデオプレーヤーが+- 40/50秒後にちらつきを開始し、+- 90秒後に黒にちらつきを開始します（Flash Player 18.0.0.200が必要）。

ステージビデオのバグを修正。

* Zendesk #3670 — 参照プレイヤーでのシーク中にVODのINVALID_PARAMETERエラーが発生しました（Flash Player 18.0.0.200が必要）

新しい期間が検出された場合、ThreadSeekでInvalidateProfilesを無効にします。

* Zendesk #3896 - Chromeで「ストリームの整合性」を「オン」に設定してFlash Playerがクラッシュする（Flash Player 18.0.0.200が必要）

Pepperでのネイティブネットワークモードでのクラッシュを修正しました。 

* Zendesk #3905 - CDNでホストされている場合、TVSDKプレイヤーが読み込まれない

pageDomainがswfドメインと異なる場合にワイルドカードトークンを検索する際の問題を修正しました。

**バージョン1.4.10** (1.4.10.642)

* Zendesk #3249 - TVSDK Web PlayerがFirefoxでFlashをクラッシュする

外部モニターで再生されるストリームがより高いビットレートストリームに切り替わると、MacのFirefoxでFlash Playerがときどきクラッシュする問題を修正しました。（Flash Player 18.0.0.160が必要）

* Zendesk #3268 — デスクトップ：ビデオプレーヤーが40/50秒 `+-` 後にちらつきを開始し、90秒後に黒くな `+-` り始める

Mac Chromeで、ストリームがちらつき始め、最終的に黒くなる問題を修正しました。 （Flash Player 18.0.0.161が必要）

* Zendesk #3304 - VAST 3.0マクロが入力さ `[ERRORCODE]` れていない

   * インライン広告に不適切なクリエイティブが含まれる場合、エラーコード400が表示されます。
   * `[ERRORCODE]` マクロはURLエンコードされます

* Zendesk #3601 — 機能強化のリクエスト：ラッパーコンパニオン管理

   * TVSDKは、リソース（html、iframeまたはstatic）を含むラッパーコンパニオンを、インラインに対して閉じるように表示します。 （インラインにそのサイズの値が含まれていない場合）
   * リソースを含むラッパーは、表示に使用しない限り無視されます。 （トラッキングには使用されません）。
   * 追跡の目的では、NOリソースを持つラッパーのコンパニオンのみが使用されます。 （トラッキングのみを含むラッパコンパニオン）

**バージョン1.4.9**

* Zendesk #2615 - HLSビューをデスクトップディスプレイから削除する際の問題

clearVideo()メソッドがMediaPlayerに追加されました。 StageVideoオブジェクトからAVStreamをクリアして、表示されているビデオフレームをクリアします。 ビデオが一時停止中で、replaceCurrentResourceまたはreplaceCurrentItemが呼び出されてからplay()を再度呼び出す必要がある場合にのみ呼び出す必要があります。 

* Zendesk #3169 - Adobe Analyticsの統合に伴うリファレンスプレイヤーの更新

リファレンスプレイヤーがAdobe Analytics統合で更新されました

* Zendesk #3296 - Desktop HLS TVSDK - VASTサードパーティの広告が再生されない

hls形式のMIMEタイプでは大文字と小文字が区別されていましたが、これは正しくなく、大文字と小文字が区別されないように変更されました。

**バージョン1.4.8**

* Zendesk #2737 - Desktop Player — エラー106000（Flash Player 17.0.0.184が必要）
* Zendesk #3007 - Flash Player 17に更新した後にプリロール広告が表示されない（Flash Player 17.0.0.184が必要）
* Zendesk #3085 - Desktop HLSプレーヤーが60秒後に106000エラーをスロー（Flash Player 17.0.0.184が必要）

**バージョン1.4.7**

* Zendesk #2760 - TrickPlayモード中にDISCONTINUITYタグが無視される（Flash Playerバージョン17.0.0.158が必要）
* Zendesk #2760 - TrickPlayモード中にDISCONTINUITYタグが無視される（Flash Playerバージョン17.0.0.158が必要）

**バージョン1.4.6**

* Zendesk #2652 - Auditude documentation for desktop HLS, Clarified Auditude media_id for desktop HLSドキュメント

**バージョン1.4.5**

* Zendesk #2256 — マスタープレイリストへのアクセス、PSDKを更新し、マスタープレイリスト上のサブスクライブ済みタグにtimedMetadataイベントをディスパッチしました。 （Flash Playerバージョン17.0.0.134が必要）
* Zendesk #2417 — 再生開始前にプレイヤーがサブタイトルをダウンロードしようとしましたが、WebVTTで、セグメント番号の一致に誤ったセグメント番号変数が使用されていました。 バグは、0から始まるセグメントインデックスを持つメディアに対してのみ表示されます。 （Flash Playerバージョン17.0.0.134が必要）
* Zendesk #2537 - ChromeでPepperプラグインを使用すると、Flash Playerがクラッシュする（Flash Playerバージョン17.0.0.134が必要）
* Zendesk #2547 — アラビア語のサブタイトル：テキストは右揃えにする必要があります（Flash Playerバージョン17.0.0.134が必要です）。

**バージョン1.4.4**

* ゼンデスク#1561 — リー： `[Adobe Primetime]` 更新：HLSクライアントベースのフェイルオーバーサポート（Desktop PSDKのPROGRAM-DATE-TIMEに対応）（Flash Playerバージョン16.0.0.305以降が必要）
* Zendesk #2197 — 広告エ `[Ads]` ラーの追跡
* Zendesk #2286 — 機能のリクエスト：広告読み込みステータスに関する情報の提供(VPAID)
* Zendesk #2285 — 機能のリクエスト：指定されたタイムアウト時間後に広告をスキップ
* バグ#3921755 - Flash Playerのバージョン1.0.1Lに対するOpenSSLライブラリの更新（Flash Playerのバージョン16.0.0.305以降が必要）

**バージョン1.4.2**

* Zendesk #1303 — クローズドキャプションの垂直方向のオフセット(Flash Playerバージョン16.0.0.235以降が必要、リリース予定日：2014年12月)
* Zendesk #1870 — クローズドキャプションのオン/オフ(Flash Playerバージョン16.0.0.235以降が必要、リリース予定日：2014年12月)
* Zendesk #2110 - VPAID広告中にフルスクリーンに入ろうとすると、再生が停止する(Flash Playerバージョン16.0.0.235以降が必要、リリース予定日：2014年12月)
* Zendesk #2199 — プレイヤーが `[VPAID]` 、広告の時間をシークする際に応答しない
* ゼンデスク#2358 — レ：チャプタ `[Analytics]` ーデータが正しくありません

**バージョン1.4.1**

* ブラックアウトAPIが更新され、AndroidおよびiOSの実装と一致するようになりました。

**バージョン1.4.0**

* Zendesk #1024 — マニフェストを介してストリームから広告を削除する機能
* Zendesk #1423:HLSの再生エラーがFlash Playerをロックしています（エラーは報告されません）。
* Zendesk #1674 - ClosedCaptionが表示されません。0x03 ETXコードがない場合は、708のキャプションが正しく表示されます。

</p>
</details>

## 既知の問題 {#known-issues}

* クローズドキャプションは、オーディオ専用のコンテンツでは機能しません。キャプションシステムは、ビデオを使用する必要があるためです。
ビデオがない場合、ビューポートの寸法はなく、ビューポートの寸法がない場合は、キャプションのグラフィックを表示できません。
* Google Chromeでは、Chromeサンドボックスの制限により、ストリームの整合性が若干低下します。
* TVSDK 1.4では、autoPlayを無効にすると、プレイヤーが1分以上アイドル状態のままの場合にDRMエラーが発生する可能性があります。 この問題を回避するには、autoPlayを無効にし、アセットを事前に読み込んだ場合、次の内 `ReferenceCore.as` 容を変更して変更を加えま `onPlaybackManagerPrepared`す。

>if (_playbackManager.autoPlay) {
>_playbackManager.play();
>} else {
>_playbackManager.play();
>_playbackManager.pause();
>}

* **バージョン1.4.13** PTPLAY-8501 - VMAPがトランスコードされていない2つのMP4直接広告を返す場合、同じフォールバック広告が2回再生されます。

* **バージョン1.4.2** Flash Playerバージョン16リリースでは、プレーヤーが空のバッファリングイベントに入った後、ABRの「切り替え中」ロジックに問題が見つかりました。 この問題により、プレーヤーがバッファリング状態になった後に、不正な帯域幅環境でビットレートが切り替わることがなくなります。 この問題を回避するには、（イベント時の）バッファリング状態の間に一時的にアプリケーションを同じに設定し、その後、イベント時の設定値に戻すようにアプリケーションを設定 `BufferControlParameters.initialBufferTime` する必要があり `BufferControlParameters.playbackBufferTime``BufferEvent.BUFFERING_BEGIN``BufferEvent.BUFFERING_END` ます。 この問題の修正は、次回のFlash Playerバージョン16のパッチリリースで利用できます。

* **バージョン1.4.0**

   * PTPLAY-1634 — 同じサブスクライブ済みタグに、異なるライブウィンドウ内の異なるタイムスタンプが含まれています。 ライブウィンドウを移動する場合、各ウィンドウ内の同じタグに同じタイムスタンプが割り当てられている必要があります。 ただし、同じタグに異なるタイムスタンプが付いている場合もあります。
   * PTPLAY-28:MediaPlayerのタイムラインに空の時間が含まれません。
   * 別のドメインからコンテンツをストリーミングする権限には、クロスドメインポリシーファイル(crossdomain.xml)が必要です。 [HTTPストリーミング用のcrossdomain.xmlファイルの設定](https://www.adobe.com/devnet/adobe-media-server/articles/cross-domain-xml-for-streaming.html)。
   * バグ#3694203:DVRストリームで、再生中のミッドロール内から別のミッドロール広告キューにシークすると、ブラウザーがフリーズする場合があります。
   * バグ#3753725 — 広告の時間が視聴されている場合、selectPolicyForSeekIntoAdが考慮されません。
   * バグ3754529：ライブDVRストリームでシークバックする際に、プリロール広告がストリームから削除されません。
   * バグ#3761896 — 広告の再生中にシークが許可された場合、広告マーカーはシーク後に再調整されます。 回避策は、シーク中にITEM_UPDATEDコールバックを使用しないことです
   * バグ#3779889 — ビデオ分析でトリック再生で最後に到達した場合、完了呼び出しが行われません。
   * バグ#VA-779 — ビットレート変更イベントハートビートは、ハートビートサポートリファレンス実装を使用した拡張ビデオ分析用に送信されません。サンプルアプリケーションにはトリック再生が実装されていません。

## 役立つリソース {#helpful-resources}

* 完全なヘルプドキュメントは、 [Adobe Primetimeのラーニングとサポートページで参照してください](https://helpx.adobe.com/support/primetime.html) 。
